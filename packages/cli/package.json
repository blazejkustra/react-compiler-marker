{
  "name": "react-compiler-marker",
  "version": "0.0.1",
  "description": "CLI for generating React Compiler reports",
  "main": "./out/main.js",
  "bin": {
    "react-compiler-marker": "./bin/cli.js"
  },
  "files": [
    "bin",
    "out",
    "README.md"
  ],
  "scripts": {
    "build": "BUILD_TARGET=cli node ../../esbuild.js --production"
  }
}
